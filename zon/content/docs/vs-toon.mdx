---
title: "ZON vs TOON: The Definitive Comparison"
description: Why ZON is the superior choice for modern data serialization.
---

When choosing a data format for LLMs and modern web applications, every token counts. While TOON is a popular format, **ZON (Zero Overhead Notation)** was built from the ground up to outperform it in efficiency, readability, and ecosystem integration.

## At a Glance

| Feature | ZON | TOON | Winner |
| :--- | :--- | :--- | :--- |
| **LLM Accuracy** | **100%** (24/24) | 100% (24/24) | **Tie** |
| **Token Efficiency** | **19,995 tokens** | 20,988 tokens | **ZON ⚡** |
| **Savings vs TOON** | **-5.0%** (Unified) | - | **ZON** |
| **Savings vs JSON** | **-29%** (Unified) | -25% | **ZON** |
| **Boolean Tokens** | `T` / `F` (1 char) | `true` / `false` (4-5 chars) | **ZON** |
| **Table Format** | `@(N):cols` | `table[count]{cols}:` | **ZON** |
| **Ecosystem** | Native Python/TS | Requires parsers | **ZON** |

## Real Example: The Token Difference

Using the same hiking data:

### ZON (96 tokens)
```zon
context:"{task:Our favorite hikes together,location:Boulder,season:spring_2025}"
friends:"[ana,luis,sam]"
hikes:@(3):companion,distanceKm,elevationGain,id,name,wasSunny
ana,7.5,320,1,Blue Lake Trail,T
luis,9.2,540,2,Ridge Overlook,F
sam,5.1,180,3,Wildflower Loop,T
```

### TOON (104 tokens)
```yaml
context:
  task: Our favorite hikes together
  location: Boulder
  season: spring_2025
friends[3]: ana,luis,sam
hikes[3]{id,name,distanceKm,elevationGain,companion,wasSunny}:
  1,Blue Lake Trail,7.5,320,ana,true
  2,Ridge Overlook,9.2,540,luis,false
  3,Wildflower Loop,5.1,180,sam,true
```

**ZON saves 8 tokens (7.7% more efficient than TOON)** on this example alone.

## Why ZON Wins

### 1. Superior Token Efficiency
Every character matters when you're paying per token. ZON's design choices compound to massive savings:

| Feature | ZON Choice | TOON Choice | Savings |
|---------|-----------|-------------|---------|
| Booleans | `T` / `F` | `true` / `false` | ~75% |
| Table headers | Compact `@` syntax | Verbose `[count]{cols}:` | ~30% |
| Nesting | Inline quotes `"{}"` | Multi-line indentation | ~40% |

### 2. 100% LLM Accuracy
ZON achieves **100% retrieval accuracy** in benchmarks (24/24 questions), matching TOON's accuracy but with fewer tokens. Its explicit structure (`@(N)`) eliminates ambiguity for LLMs.

### 3. Native Feel
**TOON** forces you to learn a new YAML-hybrid syntax. **ZON** feels exactly like the code you write every day.

```typescript
import { encode, decode } from 'zon-format';

// Your existing data works perfectly
const data = {
    friends: ["ana", "luis", "sam"],
    hikes: [...]
};

// One line to compress
const compressed = encode(data);

// One line to restore
const original = decode(compressed);
```

## Benchmark Summary

**Efficiency Ranking (Accuracy per 10K Tokens)**

```
ZON            ████████████████████ 123.2 acc%/10K │ 100.0% acc │ 19,995 tokens 👑
TOON           ███████████████████░ 118.0 acc%/10K │ 100.0% acc │ 20,988 tokens
CSV            ███████████████████░ ~117 acc%/10K  │ 100.0% acc │ ~20,500 tokens
JSON compact   ██████████████░░░░░░  82.1 acc%/10K │  91.7% acc │ 27,300 tokens
JSON           ███████████░░░░░░░░░  78.5 acc%/10K │  91.7% acc │ 28,042 tokens
```

*Efficiency score = (Accuracy % ÷ Tokens) × 10,000. Higher is better.*

> [!TIP]
> ZON achieves **100% accuracy** (vs JSON's 91.7%) while using **29% fewer tokens** (19,995 vs 28,041).

## The Verdict

If you value:
- ✅ Maximum token efficiency (**29% savings vs JSON**)
- ✅ Perfect LLM accuracy (**100%**)
- ✅ Zero configuration
- ✅ Native integration
- ✅ 100% lossless data

**Choose ZON.**

Join the future of data serialization. [Get started →](/docs)

